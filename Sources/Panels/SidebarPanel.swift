import SwiftUI

/// Pannelli SwiftUI isolati (sidebar, dashboard, settings), ognuno montato in un NSHostingView
/// proprio, disaccoppiati dal view tree del terminale. Stub.
public struct SidebarPanel: View {
    public init() {}

    public var body: some View {
        Text("Sidebar")
            .padding()
    }
}
